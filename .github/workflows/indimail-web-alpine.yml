name: indimail-web:alpine container image

on:
  push:
    paths:
      - '**/webmail/alpine'
      - .github/workflows/indimail-web-alpine.yml
      - '!**.md'
  workflow_dispatch:

jobs:
  build_and_push:
    name: build and push container image
    runs-on: ubuntu-latest
    steps:
      - name: checkout repository
        uses: actions/checkout@v2

      - name: login
        run: echo "${{ secrets.GHCR_TOKEN }}" | docker login --username "${{ github.repository_owner }}" --password-stdin ghcr.io

      - name: build
        run: |
          cp webmail/alpine/Dockerfile .
          docker build --tag "ghcr.io/${{ github.repository_owner }}/indimail-web:alpine" .

      - name: push
        run: docker push "ghcr.io/${{ github.repository_owner }}/indimail-web:alpine"
