name: indimail:gentoo container image

on:
  push:
    paths:
      - '**/indimail/gentoo'
      - .github/workflows/indimail-gentoo.yml
      - '!**.md'
  workflow_dispatch:

jobs:
  build_and_push:
    name: build and push container image
    runs-on: ubuntu-latest
    steps:
      - name: checkout repository
        uses: actions/checkout@v2

      - name: login
        run: echo "${{ secrets.GHCR_TOKEN }}" | docker login --username "${{ github.repository_owner }}" --password-stdin ghcr.io

      - name: build
        run: |
          cp indimail/gentoo/Dockerfile .
          docker build --tag "ghcr.io/${{ github.repository_owner }}/indimail:gentoo" .

      - name: push
        run: docker push "ghcr.io/${{ github.repository_owner }}/indimail:gentoo"
