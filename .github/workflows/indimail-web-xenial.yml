name: indimail-web:xenial container image

on:
  push:
    paths:
      - '**/webmail/xenial/**'
      - .github/workflows/indimail-web-xenial.yml
      - '!**.md'
  workflow_dispatch:

jobs:
  build_and_push:
    name: build and push container image
    runs-on: ubuntu-latest
    steps:
      - name: checkout repository
        uses: actions/checkout@v2

      - name: login
        run: |
          echo "${{ secrets.GHCR_TOKEN }}" | docker login ghcr.io   --username "${{ github.repository_owner }}" --password-stdin
          echo "${{ secrets.DOCKER_PAT }}" | docker login docker.io --username cprogrammer --password-stdin

      - name: build
        run: |
          cp webmail/xenial/Dockerfile .
          docker build --tag "ghcr.io/${{ github.repository_owner }}/indimail-web:xenial" .

      - name: push
        run: |
          docker push "ghcr.io/${{ github.repository_owner }}/indimail-web:xenial"
          docker image tag "ghcr.io/${{ github.repository_owner }}/indimail-web:xenial" docker.io/cprogrammer/indimail-web:xenial
          docker push docker.io/cprogrammer/indimail-web:xenial
